# Быстрый старт бота "Добро в MAX"

## 🚀 Установка и запуск

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте переменные окружения

Файл `.env` уже создан с вашим токеном. Проверьте настройки:

```env
BOT_TOKEN=f9LHodD0cOIHz3fF1nZgWFrfxDMetiYgi9RnPjsxHnzR0MD6rtu47m2gfskH7mMUT0qK0-yyPHQbYpjWFnYw
WEBHOOK_URL=https://your-domain.com
MAX_API_URL=https://api.max.ru
PORT=3000
DB_PATH=./data/dobro.db
```

### 3. Проверьте подключение к API

```bash
node scripts/test-bot.js
```

Эта команда проверит, что токен работает и бот может подключиться к MAX API.

### 4. Для локальной разработки (с ngrok)

Если вы разрабатываете локально, используйте ngrok для создания публичного URL:

1. Установите ngrok: https://ngrok.com/
2. Запустите ngrok:
   ```bash
   ngrok http 3000
   ```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Обновите `.env`:
   ```env
   WEBHOOK_URL=https://abc123.ngrok.io
   ```

### 5. Запустите бота

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или для продакшна
npm start
```

### 6. Установите webhook

После запуска бота и настройки WEBHOOK_URL, установите webhook:

```bash
npm run setup-webhook
```

Или откройте в браузере:
```
http://localhost:3000/set-webhook/f9LHodD0cOIHz3fF1nZgWFrfxDMetiYgi9RnPjsxHnzR0MD6rtu47m2gfskH7mMUT0qK0-yyPHQbYpjWFnYw
```

## 📱 Тестирование бота

1. Найдите вашего бота в мессенджере MAX
2. Отправьте команду `/start`
3. Попробуйте создать заявку через `/create`
4. Просмотрите каталог через `/catalog`

## 🔧 Структура проекта

```
.
├── server.js              # Главный сервер
├── config.js              # Конфигурация
├── bot/
│   ├── api.js             # API клиент
│   ├── handlers/          # Обработчики команд
│   ├── keyboards/          # Клавиатуры
│   ├── messages/          # Шаблоны сообщений
│   └── database/          # База данных
├── scripts/
│   ├── test-bot.js        # Тест подключения
│   └── setup-webhook.js   # Установка webhook
└── data/                  # База данных SQLite
```

## 🐛 Отладка

Если бот не отвечает:

1. Проверьте логи сервера
2. Убедитесь, что webhook установлен правильно
3. Проверьте, что сервер доступен из интернета (для webhook)
4. Используйте `node scripts/test-bot.js` для проверки API

## 📝 Команды бота

- `/start` - Главное меню
- `/catalog` - Каталог заявок
- `/create` - Создать заявку
- `/profile` - Мой профиль
- `/help` - Справка

## 🚢 Деплой

Для продакшна используйте:
- Heroku
- Railway
- Render
- VPS с Node.js

Не забудьте:
1. Установить переменные окружения
2. Указать правильный WEBHOOK_URL
3. Установить webhook после деплоя



